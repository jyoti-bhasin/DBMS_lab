//select distinct values of a column
SELECT DISTINCT age FROM EMPLOYEE

//to apply a condition for input data
create table emp2(
empid int,
ename varchar(50),
city varchar(50),
salary int,

check(salary>2000))
